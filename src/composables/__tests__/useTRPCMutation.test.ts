import { describe, expect, it} from 'vitest'

import { useTRPCMutation } from '../'

describe('useTRPCMutation', () => {
  it('should be defined', () => {
    expect(useTRPCMutation).toBeDefined()
  })
})